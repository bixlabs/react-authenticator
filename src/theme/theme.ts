const lightType: "light" | "dark" | undefined = "light";
const darkType: "light" | "dark" | undefined = "dark";

export const light = {
  palette: {
    type: lightType,
  },
};

export const dark = {
  palette: {
    type: darkType,
  },
};
