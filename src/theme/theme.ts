type Theme = "light" | "dark";
export const LIGHT: Theme = "light";
export const DARK: Theme = "dark";

export const light = {
  palette: {
    type: LIGHT,
  },
};

export const dark = {
  palette: {
    type: DARK,
  },
};
